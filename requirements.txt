# Day 1 RAG System Requirements - Following Exact Specifications
qdrant-client>=1.7.0
torch>=2.0.0
transformers>=4.35.0

# NVIDIA nv-embed (as specified in requirements)
# Note: This may require special installation from NVIDIA
sentence-transformers>=2.2.2  # Fallback until nv-embed is available

# Document processing
pdfplumber>=0.9.0
pytesseract>=0.3.10
pdf2image>=3.1.0
Pillow>=10.0.0

# Core utilities
python-dotenv>=1.0.0
tqdm>=4.65.0
numpy>=1.24.0
tiktoken>=0.5.0

# API (Flask as per original requirements)
flask>=2.3.0
flask-cors>=4.0.0

# Development
pytest>=7.4.0
